# Analytics Dashboard - Environment Variables Template
# Copy this file to .env.local and update with your values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Service role key for advanced features (admin operations)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# =============================================================================
# WEBSOCKET CONFIGURATION  
# =============================================================================
# Local development WebSocket server
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8080

# Production WebSocket server (update for deployment)
# NEXT_PUBLIC_WEBSOCKET_URL=wss://your-websocket-server.com

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================
NODE_ENV=development

# =============================================================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# =============================================================================
# Custom API endpoints (if extending the application)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Analytics tracking (if adding external analytics)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# Feature flags (for A/B testing or feature rollouts)
# NEXT_PUBLIC_ENABLE_AI_SUMMARY=true
# NEXT_PUBLIC_ENABLE_EXPORT_FEATURES=true
# NEXT_PUBLIC_ENABLE_SHARING=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Create a Supabase project at https://supabase.com
# 3. Update the SUPABASE_URL and SUPABASE_ANON_KEY with your project values
# 4. Start the WebSocket server: node websocket-server.js
# 5. Start the Next.js app: npm run dev
# 6. Visit http://localhost:3000 to see the dashboard

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# For Vercel/Netlify deployment:
# 1. Add all NEXT_PUBLIC_* variables to your deployment platform
# 2. Update WEBSOCKET_URL to point to your production WebSocket server
# 3. Ensure Supabase project is accessible from your domain
# 4. Set NODE_ENV=production for production builds